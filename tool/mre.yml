# yaml-language-server: $schema=nodes_schema.json
the_three_of_us:
  first:
  second:
  third:
    prop3: string

the_one_of_us:
  first:

first:
  prop1: first.helper
  helper:
    someval: string+
    nested_helper:
second:
  prop2: string

a:
  prop1: string
